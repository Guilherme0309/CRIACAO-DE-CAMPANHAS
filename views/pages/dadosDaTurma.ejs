<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head") %>

<body>
  <%- include("../partials/menu") %>
  <br><br>
  <a href="/tabGeral/<%= idCampanha%>/1"><img src="../../static/voltar.png" width="40px" / style="margin: 10px;"></a>
  <br>
  <center>
    <div>
      <h1 id="h1Login">TURMA: <%= dados[0].sigla %></h1>
      <h2>Docente: <%= dados[0].docente %></h2>
      <br>
      <main id="MainDash">
        <table>
          <thead>
            <tr>
              <th>Tipos de Roupa</th>
              <th>Quantidade Arrecadada</th>
              <th>Pontos Ganhos</th>
            </tr>
          </thead>
          <tbody>
            <% let i = 0; for (i = 0; i< itens.length; i++) {%>
            <tr>
              <td><%= itens[i].Descricao %> | <%= itens[i].Pontos%> Pontos</td>
              <td>
                <!-- FAZ A SOMA CASO EXISTAM MAIS ARRECADAÇÕES DE UMA MESMA ROUPA -->
                <% let qtd_itens = 0 %>
                <% for (let x = 0; x < dados.length; x++) {%>
                <% if (itens[i].Descricao == dados[x].Descricao) {%>
                <% qtd_itens += Number(dados[x].qtd) %>
                <% } %>
                <% } %>
                <!-- EXIBE QTD itens -->
                <%= qtd_itens %>
              </td>
              <td>
                <!-- FAZ A SOMA CASO EXISTAM MAIS ARRECADAÇÕES DE UMA MESMA ROUPA -->
                <% let qtd_pontos = 0 %>
                <% for (let x = 0; x < dados.length; x++) {%>
                <% if (itens[i].Descricao == dados[x].Descricao) {%>
                <% qtd_pontos += Number(dados[x].totalPontos) %>
                <% } %>
                <% } %>
                <!-- EXIBE QTD itens -->
                <%= qtd_pontos %>
              </td>
            </tr>
            <% } %>
          </tbody>
          <tfoot>
            <td colspan="3">
              PONTOS GANHOS:
              <% let total_pontos = 0 %>
              <% for (let x = 0; x < dados.length; x++) {%>
              <% total_pontos += Number(dados[x].totalPontos) %>
              <% } %>
              <%= total_pontos %>
            </td>
          </tfoot>
        </table>
      </main>
    </div>
  </center>
  <br><br>
  <%- include("../partials/footer") %>
</body>

</html>