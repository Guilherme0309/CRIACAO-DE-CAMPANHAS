<header>
    <table>
        <tr>
            <td>
                <img src="../../static/Logo.png" width="170px">
            </td>
            <td id="logout">
                <br><br>
                <% if (req.session.loggedin) { %>
                <a href="/logout">Sair da Conta</a>
                <% } else { %>
                <a href="/login">Fazer Login</a>
                <% } %>
            </td>
        </tr>
    </table>    
</header>